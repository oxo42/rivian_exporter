#!/bin/bash

SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )
REPO_DIR="$SCRIPT_DIR/.."
VENV="$REPO_DIR/.venv/bin"

source $VENV/activate
cd "$REPO_DIR/src"
python -m rivian_exporter $*